# Local development MQTT broker config for MSECS Phase 1.
# Anonymous access is enabled ONLY for local dev convenience — a production
# deployment must configure per-device credentials / TLS client certificates.
listener 1883
allow_anonymous true
persistence true
persistence_location /mosquitto/data/
log_dest stdout
